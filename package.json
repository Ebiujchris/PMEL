{
  "name": "pmel",
  "version": "1.0.0",
  "description": "PMEL Static Website",
  "main": "index.html",
  "scripts": {
    "start": "live-server",
    "dev": "live-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
